# 📊 Forex Quiz — Telegram Bot + Mini App

## 📝 Кратко

- **Как начать общение с ботом**: найдите бота в Telegram, нажмите `/start` — бот отправит приветствие и клавиатуру с кнопкой **«📊 Открыть Forex Quiz»** и пунктом **«ℹ️ О боте»**.
- **Какие функции реализованы**: прохождение квиза по форексу и инвестициям с подсветкой правильных/неправильных ответов, подсчётом итогового результата и формой обратной связи (имя + сообщение), которая сохраняется в Supabase.
- **Как работает мини‑приложение**: по нажатию web‑app‑кнопки Telegram открывает `miniapp/index.html`, который через Supabase JS SDK читает вопросы из таблицы `quiz_questions`, сохраняет результаты в `quiz_results` и отправленную форму в `feedback`.

Два независимых компонента:

- **bot/** — Telegram бот (Node.js + Telegraf)
- **miniapp/** — один HTML файл (Supabase JS SDK, без сервера)

## 🗂 Структура

```
forex-quiz-v2/
├── bot/
│   ├── src/
│   │   ├── bot.js
│   │   ├── config/config.js
│   │   ├── handlers/start.js
│   │   └── utils/keyboards.js
│   ├── package.json
│   └── .env.example
└── miniapp/
    └── index.html     ← весь квиз в одном файле
```

## ⚡️ Как это работает

```
/start в боте
      ↓
Кнопка [📊 Открыть Forex Quiz]  (webApp button)
      ↓
Telegram открывает index.html
      ↓
Supabase JS SDK (прямо в браузере):
  • читает вопросы  → quiz_questions
  • пишет результат → quiz_results
  • пишет feedback  → feedback
```

## 🗄️ БД (3 таблицы)

| Таблица          | Что хранит        |
| ---------------- | ----------------- |
| `quiz_questions` | 10 вопросов       |
| `quiz_results`   | результаты квизов |
| `feedback`       | обратная связь    |

## ⚠️ Безопасность

`anon key` Supabase — публичный ключ, специально созданный для использования
на фронтенде. Он не даёт доступа к чужим данным.
